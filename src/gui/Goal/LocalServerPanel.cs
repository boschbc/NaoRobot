using System.Windows.Forms;

namespace Naovigate.GUI.Goal
{
    /// <summary>
    /// A simple panel containing the local server control and a label.
    /// </summary>
    public sealed partial class LocalServerPanel : UserControl
    {
        public LocalServerPanel()
        {
            InitializeComponent();
        }
    }
}
